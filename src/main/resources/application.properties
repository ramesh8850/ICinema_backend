spring.application.name=icinema

#Change these settings according to database you are using
spring.datasource.url=jdbc:mysql://localhost:3306/icinema_db

spring.datasource.username=ramesh8850

#If MySQL installation is password proctored,then use below property to set password
spring.datasource.password=R@mesh8850


spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080
spring.jpa.hibernate.ddl-auto=update

# Disable security for frontend testing. Set to true to re-enable authentication.
# security.enabled=false

# Default to localhost for development. In production, OVERRIDE this with your actual frontend URL.
# Example: https://icinema-frontend.onrender.com
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:4200}

# Secret key for signing JWTs.
# In production, OVERRIDE this environment variable (JWT_SECRET) with a strong, random string.
# Default value is provided for local development convenience ONLY.
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

# Redis Configuration
spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Email Configuration (Dev: Mailhog)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=rameshgoud8850@gmail.com
spring.mail.password=eyafykjyezqtzpog
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
